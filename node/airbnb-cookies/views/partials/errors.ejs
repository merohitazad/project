<% if (typeof errors !== "undefined" && errors.length > 0) { %>
  <div class="col-span-full bg-red-100 text-red-700 p-4 rounded-lg mb-4">
    <ul class="list-disc list-inside">
      <% errors.forEach(error => { %>
        <li><%= error %></li>
      <% }) %>
    </ul>
  </div>
<% } %>